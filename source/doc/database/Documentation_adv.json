{
  "clamp": {
    "category": "adv",
    "args": "a, b, c",
    "description": "makes the value stay under desired values, 3 inputs(input_value, min_value, max_value)"
  },
  "normalize": {
    "category": "adv",
    "args": "a, b, c, d, e",
    "description": "returns normalized value restricted to 0 - 1. 5 inputs(input_value, min_value, max_value, new_min, new_max)"
  },
  "1D_lerp": {
    "category": "adv",
    "args": "a, b, c",
    "description": "Linear interpolation, 3 inputs(value{0-1}, a-point, b-point"
  },
  "sign": {
    "category": "adv",
    "args": "x",
    "description": "sign(x) returns -1 if x<0; returns 0 if x=0; returns 1 if x>1. 1 input(value)"
  },
  "abs_equal": {
    "category": "adv",
    "args": "a, b, c",
    "description": "absolute_equal gives Voolean True/False for 2 values if they are close enough by a certain threshold(value), 3 inputs(a_value, b_vlaue, threshold). NOTE-FLOATING POINT ERROR exists"
  },
  "apx_eql": {
    "category": "adv",
    "args": "a, b, c",
    "description": "approx_equal gives Boolean True/False for 2 values if they are 'close enough' or not by a threshold(relative), 3 inputs(a_value, b_value, threshold)"
  },
  "percent_equal": {
    "category": "adv",
    "args": "a, b, c",
    "description": "percent_equal gives Boolean True/False for 2 values if they are 'close enough' or not by a threshold in percent, 3 inputs(a_value, b_value, threshold)"
  },
  "in_range": {
    "category": "adv",
    "args": "a, b, c",
    "description": "in_range tell if value(x) is between low_value, and high_value, such that; low<x<high; if yes, then TRUE, else FALSE. 3 inputs(value, low_value, high_value)."
  },
  "wrap": {
    "category": "adv",
    "args": "a, b, c",
    "description": "value x to a specific range [min, max] by wrapping it back to the start if it exceeds the maximum, or back to the end if it falls below the minimum, 3 inputs(value, min, max)"
  }
}
